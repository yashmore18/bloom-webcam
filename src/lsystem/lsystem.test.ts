import { describe, it, expect } from "vitest";

// Placeholder so `npm test` is wired and green from Part 1. The real L-system
// unit tests land in Part 3 alongside src/lsystem/lsystem.ts.
describe("toolchain smoke test", () => {
  it("runs vitest", () => {
    expect(1 + 1).toBe(2);
  });
});
